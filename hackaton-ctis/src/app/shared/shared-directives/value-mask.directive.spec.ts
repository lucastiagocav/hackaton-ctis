import { ValueMaskDirective } from './value-mask.directive';

describe('ValueMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueMaskDirective();
    expect(directive).toBeTruthy();
  });
});
